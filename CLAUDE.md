# 퍼포먼스 마케팅 데이터 규칙

## 데이터 구조

```
data/
├── channel/       # *_channel.csv   — 채널 집행 데이터
├── appsflyer/     # *_appsflyer.csv — 앱스플라이어 전환 데이터
└── output/        # 조인·분석 결과 저장
```

파일명 규칙: `YYYY-MM-DD_소스명.csv`

## 채널 ↔ 미디어소스 매핑 (확정)

| 채널 데이터 (`채널`) | AF 데이터 (`미디어소스`) | 채널분류 |
|---------------------|------------------------|---------|
| 구글                | googleadwords_int      | 외부     |
| 메타                | Facebook Ads           | 외부     |
| 네이버              | naver_search           | 자체     |

## 소재명 구조

형식: `[포맷]_[카테고리]_[시즌]_[변형]_[버전]`

예시: `VID_플러스멤버십_겨울_A_v1`

| 파트 | 규칙 | 값 |
|------|------|----|
| 포맷 | 앞 3글자 | VID(영상) / IMG(이미지) / CRS(카루셀) / TXT(텍스트) |
| 카테고리 | 소재 주제 | 플러스멤버십, 할인쿠폰, 적립혜택, 배송혜택, 특가, 신상품 등 |
| 시즌 | 계절 | 봄 / 여름 / 가을 / 겨울 |
| 변형 | A/B 테스트 | A=기존 / B=신규 테스트 |
| 버전 | 수정 이력 | v1, v2, v3 … |

## 조인 규칙

- 조인 키: `일` + `미디어소스(=채널 매핑)` + `캠페인` + `그룹` + `소재`
- 방식: left join (채널 데이터 기준, AF 미매칭 행은 null 허용)
- 전환 기준: **앱스플라이어 우선** — 채널 데이터의 `구매`, `회원가입`은 참고용
- 인코딩: utf-8-sig
